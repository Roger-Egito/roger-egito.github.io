/**
 * Colors that have to be handed to something outside our CSS — a meta tag, a
 * third-party embed — and so can't be read from a custom property.
 *
 * Keep in step with src/styles/tokens.css.
 */

/** --color-bg. */
export const pageBackground = '#151515';
